
#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> v{10,20,30,40};
    v.clear();
    if(v.empty()==true)
    {
        cout<<"Vector is empty";
    }
    else
    {
        cout<<"Vector is not empty";
    }
    return 0;
}
